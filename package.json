{
  "name": "contentshield-ai",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "ContentShield AI — Production-Grade Content Protection Platform",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node -e \"fetch('http://localhost:5000/api/health').then(r=>r.json()).then(d=>console.log('✅ Health:',d)).catch(e=>console.log('❌',e.message))\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^11.9.1",
    "compression": "^1.8.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "multer": "^2.1.1",
    "socket.io": "^4.8.3",
    "uuid": "^11.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
